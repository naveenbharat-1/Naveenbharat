# Trimmed Locales

PDF.js ships 110+ language translations (~1.7 MB). Naveen Bharat only needs
en-US (default) and hi-IN, so the rest were removed. The viewer falls back
to English automatically for any unsupported locale.

Do NOT re-add the full locale tree on PDF.js upgrades.
